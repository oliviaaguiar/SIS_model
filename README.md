# SIS_model
This is the repository for creating an age-structured SIS model to describe white-nose syndrome dynamics

Model_V.1 is a first attempt at SIS modeling, using ODE methods

experimenting is a first attempt at SIS modeling using difference equation methods and arrays

Parameter_valuesV.1 is a file containing the parameter values used in the experimenting model